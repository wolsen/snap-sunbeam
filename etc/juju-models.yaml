#
# Contains the configuration for the juju kubernetes model to use
# and/or expect. This will define which cloud, etc.
#

control-plane:
  cloud: microk8s
  model: openstack
  deploy-timeout: 900

data-plane:
  cloud:
  model:
